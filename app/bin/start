#!/bin/env bash


cd "$(dirname "$0")"/..

if test -f thin.pid; then
  thin stop -P thin.pid
fi

# Enable Just-in-Time compiler
RUBY_YJIT_ENABLE=1 \
thin start -P thin.pid -p 4568 -d -l thin.log -a 0.0.0.0 -e production --threaded
